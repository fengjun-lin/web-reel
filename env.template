# Example Environment Configuration
# Copy this file to .env.local and fill in your actual values

# ============================================
# Jira Configuration (for ticket creation)
# ============================================
# Required for creating Jira tickets from replay sessions
VITE_JIRA_API_KEY=your_jira_api_token_here
VITE_JIRA_DOMAIN=your-domain.atlassian.net
VITE_JIRA_USER_EMAIL=your.email@example.com
VITE_JIRA_PROJECT_KEY=PROJ

# How to get Jira API token:
# 1. Log in to https://id.atlassian.com/manage-profile/security/api-tokens
# 2. Click "Create API token"
# 3. Copy the generated token

# ============================================
# OpenAI Configuration (optional)
# ============================================
# Required for AI-powered session analysis
VITE_OPENAI_API_KEY=sk-your-openai-api-key-here
VITE_OPENAI_API_BASE=https://api.openai.com/v1
VITE_OPENAI_MODEL=gpt-4o-mini

# Alternative models you can use:
# - gpt-4o-mini (default, fast and cheap)
# - gpt-4o (more capable, slower, more expensive)
# - gpt-3.5-turbo (fastest, cheapest, less capable)

# ============================================
# Security Note
# ============================================
# NEVER commit .env.local to version control!
# It's already in .gitignore for your safety.

