# Example Environment Configuration for Next.js
# Copy this file to .env.local and fill in your actual values

# ============================================
# Jira Configuration (for ticket creation)
# ============================================
# Required for creating Jira tickets from replay sessions

# Client-side variables (NEXT_PUBLIC_ prefix exposes to browser)
NEXT_PUBLIC_JIRA_DOMAIN=your-domain.atlassian.net
NEXT_PUBLIC_JIRA_PROJECT_KEY=PROJ

# Server-side only variables (NOT exposed to browser - more secure)
JIRA_API_KEY=your_jira_api_token_here
JIRA_USER_EMAIL=your.email@example.com

# How to get Jira API token:
# 1. Log in to https://id.atlassian.com/manage-profile/security/api-tokens
# 2. Click "Create API token"
# 3. Copy the generated token

# ============================================
# OpenAI Configuration (optional)
# ============================================
# Required for AI-powered session analysis

# Client-side variables
NEXT_PUBLIC_OPENAI_API_BASE=https://api.openai.com/v1
NEXT_PUBLIC_OPENAI_MODEL=gpt-4o-mini

# Server-side only variable (more secure - API keys should be server-side)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Alternative models you can use:
# - gpt-4o-mini (default, fast and cheap)
# - gpt-4o (more capable, slower, more expensive)
# - gpt-3.5-turbo (fastest, cheapest, less capable)

# ============================================
# Security Note
# ============================================
# NEVER commit .env.local to version control!
# It's already in .gitignore for your safety.
# 
# NEXT_PUBLIC_* variables are exposed to the browser
# Variables without NEXT_PUBLIC_ prefix are server-side only

